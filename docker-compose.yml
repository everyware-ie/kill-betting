services:
  backend:
    build:
      context: ./backend
    image: ghcr.io/everyware-ie/kill-betting-backend:${IMAGE_TAG:-latest}
    container_name: killnagi-backend
    env_file:
      - .env
    ports:
      - "8080:8080"
    networks:
      - default
      - killnagi-net
    volumes:
      - upload-data:/app/uploads
    restart: on-failure:3

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8080/api}
    image: ghcr.io/everyware-ie/kill-betting-frontend:${IMAGE_TAG:-latest}
    container_name: killnagi-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  prometheus:
    image: prom/prometheus:latest
    container_name: killnagi-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - killnagi-net
    restart: on-failure:3

  loki:
    image: grafana/loki:latest
    container_name: killnagi-loki
    ports:
      - "3100:3100"
    networks:
      - killnagi-net
    restart: on-failure:3

  grafana:
    image: grafana/grafana:latest
    container_name: killnagi-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin}
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards
    networks:
      - killnagi-net
    depends_on:
      - prometheus
      - loki
    restart: on-failure:3

volumes:
  upload-data:
  grafana-data:

networks:
  killnagi-net:
    external: true
    name: killnagi-net